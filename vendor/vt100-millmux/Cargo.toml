# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.78"
name = "millrace-terminal-vt100"
version = "0.16.2"
authors = ["Jesse Luehrs <doy@tozt.net>"]
build = false
include = [
    "src/**/*",
    "LICENSE",
    "README.md",
    "CHANGELOG.md",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Millmux terminal parser fork with deterministic grapheme width"
homepage = "https://github.com/tim-osterhus/millmux"
readme = "README.md"
keywords = [
    "terminal",
    "vt100",
]
categories = [
    "command-line-interface",
    "encoding",
]
license = "MIT"
repository = "https://github.com/tim-osterhus/millmux"

[package.metadata.millmux]
upstream_source_version = "0.16.2"
upstream_repository = "https://github.com/doy/vt100-rust"
purpose = "parser-time grapheme width and deterministic UTF-8 finalization"

[lib]
name = "vt100"
path = "src/lib.rs"

[dependencies.itoa]
version = "=1.0.18"

[dependencies.unicode-segmentation]
version = "=1.12.0"

[dependencies.unicode-width]
version = "=0.2.0"

[dependencies.vte]
version = "=0.15.0"
